# Start of loop

# initialise loop so that it runs at least once
name = ""
count = 0
max_tickets = 5

while name!= "xxx" and count < max_tickets:

# get details
    name = input("Name:")
    count += 1
